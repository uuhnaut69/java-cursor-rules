# Controller configuration
# Set coco=true to enable CocoController (with memory leaks for demo purposes)
# Set coco=false to enable WithoutCocoController (proper resource management)
coco=true

# Server graceful shutdown configuration
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s
